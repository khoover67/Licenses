@model IEnumerable<Licenses.Areas.Tables.Models.ProductModel>

@{
    ViewBag.Title = "Index";
}

<h2>Products</h2>
<hr />
<p>
    @Html.ActionLink("Create New", "Create", new { auth = ViewBag.AuthCode })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.prod_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.prod_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.prod_id, auth = ViewBag.AuthCode }) |
            @Html.ActionLink("Details", "Details", new { id = item.prod_id, auth = ViewBag.AuthCode }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.prod_id, auth = ViewBag.AuthCode })
        </td>
    </tr>
}

</table>
